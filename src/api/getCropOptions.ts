import { TCropOptions } from "../models/TCropOptions";

// I haven't set up the API yet so I am using this as a placeholder
// import { API_URL } from "./config";

//export async function getCropOptions(): Promise<TCropOptions[]> {
//    const response = await fetch(`${API_URL}/api/projects/cropOptions`);
//    return response.json();
//}